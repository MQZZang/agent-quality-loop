---
description: >-
  Compatibility rule for ask-plan-code-qa. Apply only when the user explicitly
  invokes ask-plan-code-qa or agent-quality-loop selects it as the embedded code
  implementation adapter. Do not use it as the default lifecycle router.
alwaysApply: false
---

# Code Implementation Adapter

Full procedure: `.cursor/skills/ask-plan-code-qa/SKILL.md`.

- Standalone: explicit invocation only.
- Embedded: consume the parent `ALIGNED`/`EVIDENCED` contract; do not repeat alignment or lifecycle output.
- Inspect → plan gate → minimal code change → implementation self-QA.
- Preserve parent goal, scope, assurance, authority, baseline, and must-hold checks.
- Retrieve matching active `.ai/knowledge/lessons.md` on inspect/plan; project-level + local_write may write verified lessons with disclosed diff. Sub-executor Dispatch Brief is defined by `agent-quality-loop` in `.cursor/skills/agent-quality-loop/references/code-implementation-adapter.md` (projection of the parent contract, not a second ritual; not owned by this adapter).
- Return an implementation receipt with `result_phase: BUILT`.
- Never grant independent acceptance, release readiness, deployment, or new authority.

Review / 验收 requests route back through `agent-quality-loop` accept, which may invoke `review-gate`.
